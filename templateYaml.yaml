myStruct:
    - unsigned long long: id
    - 80: myCustomClass
    - size_t: classBloat # this is part of a list, thus it is limited by the 'size_t' directive that came before it
    - size_t: secondClassBloat # part of list, thus same size
    - float: __index__price
    - float: horario
    - float: outro
    - char: grade

# {'myStruct': [{'size_t': 'size'},
#               {'unsigned long long': 'id'},
#               {80: 'myCustomClass'},
#               {'size_t': 'classBloat'},
#               {'size_t': 'secondClassBloat'},
#               {'float': '__index__price'},
#               {'float': 'horario'},
#               {'float': 'price'},
#               {'float': 'outro'},
#               {'char': 'grade'},
#              ]}
